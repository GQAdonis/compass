# Authentic Compass 0.3.6 artifact

This graph was generated by the official `compass-v0.3.6` macOS ARM64 release,
not by editing the builder version of a newer graph. It has one source file,
three nodes, three edges, and is 6,406 bytes including a trailing newline added
when checking in the fixture (the release output is 6,405 bytes).
All graph source paths are relative.
No source-root.txt is checked in: integration tests create their own provenance.

Release archive: `compass-aarch64-apple-darwin.tar.gz` from
<https://github.com/crabbuild/compass/releases/tag/compass-v0.3.6>.
Archive SHA-256: `ed751465163ee77b4d73eb74b17d45be0d0324e38db0e9be19119be1693843fd`.

With only the committed source/lib.rs in a temporary project directory:

```sh
compass-0.3.6 extract <project> --code-only --store json --no-cluster --no-viz --out <output>
```

The `<output>/compass-out/graph.json` payload is retained without changes other
than that trailing newline. This fixture
must fail during engine loading, before adjacency queries. Recovery tests copy
the source to a fresh project, supply validated provenance, and force-rebuild it
using the current installed executable.
