# Agent query evaluation suite v2: 50 fresh questions, five repositories, five
# languages. This file is the blackbox-fair companion to `suite.toml`: it keeps
# the same pinned checkouts and source-reviewed judgments, and it adds the
# questions and the query surfaces the first suite did not measure.
#
# Fairness contract, per row:
#   * Both tools are blackboxes over the same pinned checkout. The oracle is
#     read from source only; neither tool's output is used as ground truth.
#   * Each row asks the same question of the same source declaration. The argv
#     uses the address form that each tool documents for that surface
#     (Compass: plain or qualified symbol name, file path; Graphify: node label
#     or file label). No row grants one tool an affordance the other lacks.
#   * Both tools run their closest documented operation for the question:
#     explain -> explain/explain, usage -> callers/affected, outbound calls ->
#     callees/explain, transitive dependents -> impact/affected, connectivity ->
#     path/path, discovery -> query/query, and name resolution - the
#     `ambiguity` and `negative` rows - -> search/explain, because
#     `graphify explain` is the command that reports the candidate list for a
#     name that matches several nodes and the explicit no-match for a name that
#     matches none.
#   * `path` questions pass `--undirected` to Graphify because Compass `path`
#     searches relationships in both directions; Graphify documents
#     `--undirected` as the equivalent.
#   * A `pick_list` row requires a real candidate list (at least two distinct
#     candidates) plus at least one of the reviewed candidates for the name.
#     It deliberately does not require a specific pair: a bounded page shows
#     only part of the candidate set, so demanding named candidates would score
#     which entries happen to fit rather than whether the tool offers a list.
#   * `broad` rows use a 600-token page budget for both tools, the reviewed
#     floor at which either tool can render a bounded page for these questions
#     instead of spending the whole page on its own metadata envelope.
#   * Default output form on both sides, at the same approximate token budget
#     where the command is budgeted. Continuations are each tool's documented
#     one (Compass `--cursor`, Graphify `--budget`), and the runner prices the
#     whole workflow.
#   * Oracles are the file names, declaration lines, call-site lines, and symbol
#     names the reviewer read in the pinned checkout. They are a deterministic
#     text-recall proxy, not an independent precision oracle.
#   * Where a tool's graph or command cannot produce a source-verified fact, the
#     row fails and REPORT.md counts it as a recall gap. No oracle is weakened to
#     favour either tool.
#
# Kinds:
#   explain        - locate a declaration with its file and line
#   explain_source - return the declaration source text itself
#   callers        - incoming usage, must return the callers
#   callees        - outbound calls, must return the callees
#   impact         - transitive dependents of a symbol
#   path           - symbol-to-symbol connectivity
#   file_path      - file-to-file connectivity
#   ambiguity      - short name with several candidates, must offer a pick list
#   negative       - absent symbol, must answer no-match instead of guessing
#   broad          - natural question whose answer needs bounded follow-up

schema = "compass.agent-query-suite/1"

[[repository]]
name = "cobra"
language = "Go"
url = "https://github.com/spf13/cobra.git"
commit = "adbc8813901bba65827259daa8e22ff94ec1f30e"

[[repository.anchor]]
file = "command.go"
line = 1868
symbol = "ParseFlags"
judgment = "command.go:1868 declares func (c *Command) ParseFlags(args []string) error."

[[repository.anchor]]
file = "command.go"
line = 757
symbol = "Find"
judgment = "command.go:757 declares func (c *Command) Find(args []string) (*Command, []string, error)."

[[repository.anchor]]
file = "doc/md_docs.go"
line = 52
symbol = "GenMarkdown"
judgment = "doc/md_docs.go:52 declares GenMarkdown, which consumes the cobra package it imports at line 27."

[[repository.question]]
id = "cobra2-explain-parseflags"
kind = "explain"
subject = "cobra.Command::ParseFlags"
compass = ["explain", "cobra.Command::ParseFlags"]
graphify = ["explain", ".ParseFlags()"]
expect = "answer"
required = ["command.go", "1868"]
judgment = "command.go:1868 declares func (c *Command) ParseFlags(args []string) error."

[[repository.question]]
id = "cobra2-source-find"
kind = "explain_source"
subject = "cobra.Command::Find declaration source"
compass = ["explain", "cobra.Command::Find", "--source", "--root", "."]
graphify = ["explain", ".Find()"]
expect = "answer"
required = ["func (c *Command) Find(args []string)"]
judgment = "command.go:757 declares func (c *Command) Find(args []string) (*Command, []string, error); the answer must contain that declaration, not only its location."

[[repository.question]]
id = "cobra2-callers-find"
kind = "callers"
subject = "cobra.Command::Find callers"
compass = ["callers", "cobra.Command::Find"]
graphify = ["affected", ".Find()", "--depth", "1"]
expect = "answer"
required = ["ExecuteC", "command.go"]
judgment = "Find is called by ExecuteC at command.go:1123 and by InitDefaultHelpCmd (command.go:1263) at command.go:1276 and command.go:1294."

[[repository.question]]
id = "cobra2-callees-executec"
kind = "callees"
subject = "cobra.Command::ExecuteC callees"
compass = ["callees", "cobra.Command::ExecuteC"]
graphify = ["explain", ".ExecuteC()"]
expect = "answer"
required = ["Find", "execute", "Traverse"]
judgment = "ExecuteC calls Traverse (command.go:1121), Find (command.go:1123) and execute (command.go:1148)."

[[repository.question]]
id = "cobra2-impact-parseflags"
kind = "impact"
subject = "transitive dependents of cobra.Command::ParseFlags"
compass = ["impact", "cobra.Command::ParseFlags", "--max-depth", "3"]
graphify = ["affected", ".ParseFlags()", "--depth", "3"]
expect = "answer"
max_follow_ups = 3
required = ["execute", "Traverse"]
judgment = "ParseFlags is called directly by Traverse (command.go:854), execute (command.go:919) and getCompletions (completions.go:372); execute is called by ExecuteC at command.go:1148. The oracle requires the reviewed direct callers because the depth-three closure is 945 retained dependents: requiring one specific depth-two node would reward a four-row answer over a complete one."

[[repository.question]]
id = "cobra2-path-find-legacyargs"
kind = "path"
subject = "cobra.Command::Find -> legacyArgs"
compass = ["path", "cobra.Command::Find", "cobra.legacyArgs"]
graphify = ["path", ".Find()", "legacyArgs()", "--undirected"]
expect = "answer"
required = ["Find", "legacyArgs"]
judgment = "Find returns legacyArgs(commandFound, stripFlags(a, commandFound)) at command.go:776; args.go:28 declares legacyArgs."
forbidden = ["NO PATH FOUND", "No directed path found", "No path found"]

[[repository.question]]
id = "cobra2-file-path-md-docs"
kind = "file_path"
subject = "doc/md_docs.go -> command.go"
compass = ["path", "doc/md_docs.go", "command.go"]
graphify = ["path", "md_docs.go", "command.go", "--undirected"]
expect = "answer"
required = ["md_docs.go", "command.go"]
judgment = "doc/md_docs.go imports github.com/spf13/cobra at line 27 and calls GenMarkdownCustom, which reaches the Command type declared in command.go."
forbidden = ["NO PATH FOUND", "No directed path found", "No path found"]

[[repository.question]]
id = "cobra2-ambiguous-command"
kind = "ambiguity"
subject = "Command"
compass = ["search", "Command"]
graphify = ["explain", "Command"]
expect = "pick_list"
required_one_of = ["command.go", "completions.go"]
min_one_of = 1
min_candidates = 2
judgment = "command.go:54 declares the Command type, and command.go, completions.go, bash_completions.go, fish_completions.go and flag_groups.go each hold declarations whose name begins with Command; a pick list is required."

[[repository.question]]
id = "cobra2-negative"
kind = "negative"
subject = "ZebraQuantumCommandRouter"
compass = ["search", "ZebraQuantumCommandRouter"]
graphify = ["explain", "ZebraQuantumCommandRouter"]
expect = "no_match"
judgment = "The identifier is absent from the pinned checkout; returning candidates as an answer is a false positive."

[[repository.question]]
id = "cobra2-broad-resolve-run"
kind = "broad"
subject = "how does cobra resolve a subcommand name and then run the resolved command"
compass = ["query", "how does cobra resolve a subcommand name and then run the resolved command"]
graphify = ["query", "how does cobra resolve a subcommand name and then run the resolved command"]
expect = "answer"
required = ["Find", "execute"]
budget_tokens = 600
max_follow_ups = 6
judgment = "Find (command.go:757) resolves the target command; execute (command.go:905) runs it."

[[repository]]
name = "flask"
language = "Python"
url = "https://github.com/pallets/flask.git"
commit = "d73fa1cdcbd8b1465c151db8924ba58b1dd14e35"

[[repository.anchor]]
file = "src/flask/app.py"
line = 969
symbol = "dispatch_request"
judgment = "src/flask/app.py:969 declares Flask.dispatch_request."

[[repository.anchor]]
file = "src/flask/ctx.py"
line = 260
symbol = "AppContext"
judgment = "src/flask/ctx.py:260 declares AppContext."

[[repository.anchor]]
file = "src/flask/views.py"
line = 78
symbol = "View.dispatch_request"
judgment = "src/flask/views.py:78 declares View.dispatch_request."

[[repository.question]]
id = "flask2-explain-fulldispatch"
kind = "explain"
subject = "Flask::full_dispatch_request"
compass = ["explain", "src.flask.app.Flask::full_dispatch_request"]
graphify = ["explain", ".full_dispatch_request()"]
expect = "answer"
required = ["app.py", "995"]
judgment = "src/flask/app.py:995 declares Flask.full_dispatch_request."

[[repository.question]]
id = "flask2-source-wsgiapp"
kind = "explain_source"
subject = "Flask::wsgi_app declaration source"
compass = ["explain", "src.flask.app.Flask::wsgi_app", "--source", "--root", "."]
graphify = ["explain", ".wsgi_app()"]
expect = "answer"
required = ["def wsgi_app"]
judgment = "src/flask/app.py:1569 declares def wsgi_app(self, environ, start_response); the answer must contain that declaration."

[[repository.question]]
id = "flask2-callers-fulldispatch"
kind = "callers"
subject = "Flask::full_dispatch_request callers"
compass = ["callers", "src.flask.app.Flask::full_dispatch_request"]
graphify = ["affected", ".full_dispatch_request()", "--depth", "1"]
expect = "answer"
required = ["wsgi_app"]
judgment = "wsgi_app calls self.full_dispatch_request(ctx) at src/flask/app.py:1600."

[[repository.question]]
id = "flask2-callees-fulldispatch"
kind = "callees"
subject = "Flask::full_dispatch_request callees"
compass = ["callees", "src.flask.app.Flask::full_dispatch_request"]
graphify = ["explain", ".full_dispatch_request()"]
expect = "answer"
required = ["preprocess_request", "dispatch_request", "finalize_request"]
judgment = "full_dispatch_request calls preprocess_request (app.py:1017), dispatch_request (1019) and finalize_request (1022)."

[[repository.question]]
id = "flask2-impact-fulldispatch"
kind = "impact"
subject = "transitive dependents of Flask::full_dispatch_request"
compass = ["impact", "src.flask.app.Flask::full_dispatch_request", "--max-depth", "2"]
graphify = ["affected", ".full_dispatch_request()", "--depth", "2"]
expect = "answer"
max_follow_ups = 3
required = ["wsgi_app", "__call__"]
judgment = "wsgi_app calls full_dispatch_request at app.py:1600, and Flask.__call__ (app.py:1621) calls wsgi_app at app.py:1628."

[[repository.question]]
id = "flask2-path-fulldispatch-finalize"
kind = "path"
subject = "Flask::full_dispatch_request -> Flask::finalize_request"
compass = ["path", "src.flask.app.Flask::full_dispatch_request", "src.flask.app.Flask::finalize_request"]
graphify = ["path", ".full_dispatch_request()", ".finalize_request()", "--undirected"]
expect = "answer"
required = ["full_dispatch_request", "finalize_request"]
judgment = "full_dispatch_request calls self.finalize_request(ctx, rv) at src/flask/app.py:1022, which is declared at line 1024."
forbidden = ["NO PATH FOUND", "No directed path found", "No path found"]

[[repository.question]]
id = "flask2-file-path-views-app"
kind = "file_path"
subject = "src/flask/views.py -> src/flask/app.py"
compass = ["path", "src/flask/views.py", "src/flask/app.py"]
graphify = ["path", "flask/views.py", "flask/app.py", "--undirected"]
expect = "answer"
required = ["views.py", "app.py"]
judgment = "src/flask/views.py:110 calls current_app.ensure_sync, which is Flask.ensure_sync in src/flask/app.py:1068."
forbidden = ["NO PATH FOUND", "No directed path found", "No path found"]

[[repository.question]]
id = "flask2-ambiguous-urlfor"
kind = "ambiguity"
subject = "url_for"
compass = ["search", "url_for"]
graphify = ["explain", "url_for"]
expect = "pick_list"
required_one_of = ["app.py", "helpers.py"]
min_one_of = 1
min_candidates = 2
judgment = "src/flask/app.py:1105 declares Flask.url_for and src/flask/helpers.py:200 declares the module-level url_for; a pick list is required."

[[repository.question]]
id = "flask2-negative"
kind = "negative"
subject = "ZebraQuantumMissingBlueprint"
compass = ["search", "ZebraQuantumMissingBlueprint"]
graphify = ["explain", "ZebraQuantumMissingBlueprint"]
expect = "no_match"
judgment = "The identifier is absent from the pinned checkout; returning candidates as an answer is a false positive."

[[repository.question]]
id = "flask2-broad-url-rule"
kind = "broad"
subject = "how does flask turn a view function into a url rule"
compass = ["query", "how does flask turn a view function into a url rule"]
graphify = ["query", "how does flask turn a view function into a url rule"]
expect = "answer"
required = ["add_url_rule", "scaffold.py"]
budget_tokens = 600
max_follow_ups = 6
judgment = "Scaffold.route (src/flask/sansio/scaffold.py:344) calls self.add_url_rule at line 370, and add_url_rule is declared at src/flask/sansio/app.py:605."

[[repository]]
name = "gson"
language = "Java"
url = "https://github.com/google/gson.git"
commit = "15ca7360379cf3c1502b59981569050489f2d73e"

[[repository.anchor]]
file = "gson/src/main/java/com/google/gson/Gson.java"
line = 565
symbol = "toJson(Object)"
judgment = "Gson.java:565 declares public String toJson(Object src)."

[[repository.anchor]]
file = "gson/src/main/java/com/google/gson/stream/JsonWriter.java"
line = 527
symbol = "value(String)"
judgment = "JsonWriter.java:527 declares public JsonWriter value(String value)."

[[repository.anchor]]
file = "gson/src/main/java/com/google/gson/TypeAdapter.java"
line = 143
symbol = "toJson(Writer, T)"
judgment = "TypeAdapter.java:143 declares final void toJson(Writer out, T value)."

[[repository.question]]
id = "gson2-explain-newjsonwriter"
kind = "explain"
subject = "Gson::newJsonWriter"
compass = ["explain", "com.google.gson.Gson::newJsonWriter"]
graphify = ["explain", ".newJsonWriter()"]
expect = "answer"
required = ["Gson.java", "797"]
judgment = "Gson.java:797 declares public JsonWriter newJsonWriter(Writer writer)."

[[repository.question]]
id = "gson2-source-newjsonwriter"
kind = "explain_source"
subject = "Gson::newJsonWriter declaration source"
compass = ["explain", "com.google.gson.Gson::newJsonWriter", "--source", "--root", "."]
graphify = ["explain", ".newJsonWriter()"]
expect = "answer"
required = ["public JsonWriter newJsonWriter(Writer writer)"]
judgment = "Gson.java:797 declares public JsonWriter newJsonWriter(Writer writer); the answer must contain that declaration."

[[repository.question]]
id = "gson2-callers-newjsonwriter"
kind = "callers"
subject = "Gson::newJsonWriter callers"
compass = ["callers", "com.google.gson.Gson::newJsonWriter"]
graphify = ["affected", ".newJsonWriter()", "--depth", "1"]
expect = "answer"
required = ["toJson", "Gson.java"]
judgment = "newJsonWriter is called by the toJson overloads at Gson.java:642 and Gson.java:724."

[[repository.question]]
id = "gson2-callees-newjsonwriter"
kind = "callees"
subject = "Gson::newJsonWriter callees"
compass = ["callees", "com.google.gson.Gson::newJsonWriter"]
graphify = ["explain", ".newJsonWriter()"]
expect = "answer"
required = ["setHtmlSafe", "setSerializeNulls"]
judgment = "newJsonWriter calls setFormattingStyle (Gson.java:802), setHtmlSafe (803), setStrictness (804) and setSerializeNulls (805) on the writer it returns."

[[repository.question]]
id = "gson2-impact-newjsonwriter"
kind = "impact"
subject = "transitive dependents of Gson::newJsonWriter"
compass = ["impact", "com.google.gson.Gson::newJsonWriter", "--max-depth", "2"]
graphify = ["affected", ".newJsonWriter()", "--depth", "2"]
expect = "answer"
max_follow_ups = 3
required = ["toJson"]
judgment = "newJsonWriter is reached from the Gson.toJson overloads (Gson.java:642, 724) that serialize to an Appendable."

[[repository.question]]
id = "gson2-path-newjsonwriter-jsonwriter"
kind = "path"
subject = "Gson::newJsonWriter -> JsonWriter"
compass = ["path", "com.google.gson.Gson::newJsonWriter", "com.google.gson.stream.JsonWriter"]
graphify = ["path", ".newJsonWriter()", "JsonWriter", "--undirected"]
expect = "answer"
required = ["JsonWriter"]
judgment = "newJsonWriter constructs and returns a JsonWriter (Gson.java:801), the class declared at stream/JsonWriter.java:162."
forbidden = ["NO PATH FOUND", "No directed path found", "No path found"]

[[repository.question]]
id = "gson2-file-path-typeadapter-jsonwriter"
kind = "file_path"
subject = "gson/.../TypeAdapter.java -> gson/.../stream/JsonWriter.java"
compass = ["path", "gson/src/main/java/com/google/gson/TypeAdapter.java", "gson/src/main/java/com/google/gson/stream/JsonWriter.java"]
graphify = ["path", "TypeAdapter.java", "JsonWriter.java", "--undirected"]
expect = "answer"
required = ["TypeAdapter.java", "JsonWriter.java"]
judgment = "TypeAdapter.java:143 writes through the JsonWriter declared at stream/JsonWriter.java:162, and TypeAdapter.java references the JsonWriter type directly."
forbidden = ["NO PATH FOUND", "No directed path found", "No path found"]

[[repository.question]]
id = "gson2-ambiguous-tojson"
kind = "ambiguity"
subject = "toJson"
compass = ["search", "toJson"]
graphify = ["explain", "toJson"]
expect = "pick_list"
required_one_of = ["Gson.java", "TypeAdapter.java"]
min_one_of = 1
min_candidates = 2
judgment = "Gson.java declares eight toJson overloads (565, 590, 612, 640, 670, 708, 722, 751) and TypeAdapter.java declares two (143, 160)."

[[repository.question]]
id = "gson2-negative"
kind = "negative"
subject = "ZebraQuantumMissingWriter"
compass = ["search", "ZebraQuantumMissingWriter"]
graphify = ["explain", "ZebraQuantumMissingWriter"]
expect = "no_match"
judgment = "The identifier is absent from the pinned checkout; returning candidates as an answer is a false positive."

[[repository.question]]
id = "gson2-broad-deserialize"
kind = "broad"
subject = "how does gson read json into an object"
compass = ["query", "how does gson read json into an object"]
graphify = ["query", "how does gson read json into an object"]
expect = "answer"
required = ["fromJson", "Gson.java"]
budget_tokens = 600
max_follow_ups = 6
judgment = "The reviewed entry points are the Gson.fromJson overloads (Gson.java:850 String+Class, 879 String+Type, 909 String+TypeToken, 940 Reader+Class); TypeAdapter.read is the collaborator rather than required content."

[[repository]]
name = "zod"
language = "TypeScript"
url = "https://github.com/colinhacks/zod.git"
commit = "d2b135cfb7a3582b9eb515756b9166bcb9521f4a"

[[repository.anchor]]
file = "packages/zod/src/v4/classic/schemas.ts"
line = 303
symbol = "ZodType.safeParse"
judgment = "packages/zod/src/v4/classic/schemas.ts:303 implements ZodType.safeParse."

[[repository.anchor]]
file = "packages/zod/src/v4/classic/from-json-schema.ts"
line = 105
symbol = "detectVersion"
judgment = "packages/zod/src/v4/classic/from-json-schema.ts:105 declares detectVersion."

[[repository.anchor]]
file = "packages/zod/src/v4/classic/parse.ts"
line = 22
symbol = "safeParse"
judgment = "packages/zod/src/v4/classic/parse.ts:22 exports the classic safeParse entry point."

[[repository.question]]
id = "zod2-explain-detectversion"
kind = "explain"
subject = "detectVersion"
compass = ["explain", "detectVersion"]
graphify = ["explain", "detectVersion()"]
expect = "answer"
required = ["from-json-schema.ts", "105"]
judgment = "packages/zod/src/v4/classic/from-json-schema.ts:105 declares function detectVersion."

[[repository.question]]
id = "zod2-source-convertschema"
kind = "explain_source"
subject = "convertSchema declaration source"
compass = ["explain", "convertSchema", "--source", "--root", "."]
graphify = ["explain", "convertSchema()"]
expect = "answer"
required = ["function convertSchema(schema:"]
judgment = "packages/zod/src/v4/classic/from-json-schema.ts:808 declares function convertSchema(schema, ctx); the answer must contain that declaration."

[[repository.question]]
id = "zod2-callers-detectversion"
kind = "callers"
subject = "detectVersion callers"
compass = ["callers", "detectVersion"]
graphify = ["affected", "detectVersion()", "--depth", "1"]
expect = "answer"
required = ["fromJSONSchema", "from-json-schema.ts"]
judgment = "fromJSONSchema calls detectVersion(normalized, params?.defaultTarget) at from-json-schema.ts:931."

[[repository.question]]
id = "zod2-callees-convertschema"
kind = "callees"
subject = "convertSchema callees"
compass = ["callees", "convertSchema"]
graphify = ["explain", "convertSchema()"]
expect = "answer"
required = ["convertBaseSchema"]
judgment = "convertSchema calls convertBaseSchema(schema, ctx) at from-json-schema.ts:814."

[[repository.question]]
id = "zod2-impact-detectversion"
kind = "impact"
subject = "transitive dependents of detectVersion"
compass = ["impact", "detectVersion", "--max-depth", "2"]
graphify = ["affected", "detectVersion()", "--depth", "2"]
expect = "answer"
max_follow_ups = 3
required = ["fromJSONSchema"]
judgment = "detectVersion is called by fromJSONSchema (from-json-schema.ts:917), the public JSON Schema entry point."

[[repository.question]]
id = "zod2-path-detectversion-convertschema"
kind = "path"
subject = "detectVersion -> convertSchema"
compass = ["path", "detectVersion", "convertSchema"]
graphify = ["path", "detectVersion()", "convertSchema()", "--undirected"]
expect = "answer"
required = ["detectVersion", "convertSchema"]
judgment = "fromJSONSchema calls detectVersion (from-json-schema.ts:931) and convertSchema (line 952), connecting the two."
forbidden = ["NO PATH FOUND", "No directed path found", "No path found"]

[[repository.question]]
id = "zod2-file-path-json-schema"
kind = "file_path"
subject = "classic/from-json-schema.ts -> classic/schemas.ts"
compass = ["path", "packages/zod/src/v4/classic/from-json-schema.ts", "packages/zod/src/v4/classic/schemas.ts"]
graphify = ["path", "from-json-schema.ts", "classic/schemas.ts", "--undirected"]
expect = "answer"
required = ["from-json-schema.ts", "schemas.ts"]
judgment = "from-json-schema.ts imports ./schemas.js at line 6 and builds zod schemas through it."
forbidden = ["NO PATH FOUND", "No directed path found", "No path found"]

[[repository.question]]
id = "zod2-ambiguous-safeparse"
kind = "ambiguity"
subject = "safeParse"
compass = ["search", "safeParse"]
graphify = ["explain", "safeParse"]
expect = "pick_list"
required_one_of = ["schemas.ts", "parse.ts", "types.ts"]
min_one_of = 1
min_candidates = 2
judgment = "safeParse is declared in classic/schemas.ts:303, classic/parse.ts:22, core/parse.ts:80 and v3/types.ts; a pick list is required."

[[repository.question]]
id = "zod2-negative"
kind = "negative"
subject = "ZebraQuantumMissingSchema"
compass = ["search", "ZebraQuantumMissingSchema"]
graphify = ["explain", "ZebraQuantumMissingSchema"]
expect = "no_match"
judgment = "The identifier is absent from the pinned checkout; returning candidates as an answer is a false positive."

[[repository.question]]
id = "zod2-broad-json-schema"
kind = "broad"
subject = "how does zod convert a json schema into a zod schema"
compass = ["query", "how does zod convert a json schema into a zod schema"]
graphify = ["query", "how does zod convert a json schema into a zod schema"]
expect = "answer"
required = ["fromJSONSchema", "convertSchema"]
budget_tokens = 600
max_follow_ups = 6
judgment = "fromJSONSchema (from-json-schema.ts:917) validates the version with detectVersion and converts through convertSchema (808)."

[[repository]]
name = "axum"
language = "Rust"
url = "https://github.com/tokio-rs/axum.git"
commit = "af1345b53a259b0990be1ff853f9b56c05040ef7"

[[repository.anchor]]
file = "src/routing/mod.rs"
line = 192
symbol = "Router::route"
judgment = "src/routing/mod.rs:192 declares pub fn route(self, path: &str, method_router: MethodRouter<S>) -> Self."

[[repository.anchor]]
file = "src/routing/path_router.rs"
line = 22
symbol = "validate_path"
judgment = "src/routing/path_router.rs:22 declares fn validate_path."

[[repository.anchor]]
file = "src/serve/mod.rs"
line = 106
symbol = "serve"
judgment = "src/serve/mod.rs:106 declares pub fn serve."

[[repository.question]]
id = "axum2-explain-methodfilter"
kind = "explain"
subject = "MethodFilter"
compass = ["explain", "MethodFilter"]
graphify = ["explain", "MethodFilter"]
expect = "answer"
required = ["method_filter.rs", "9"]
judgment = "src/routing/method_filter.rs:9 declares pub struct MethodFilter."

[[repository.question]]
id = "axum2-source-validate-path"
kind = "explain_source"
subject = "validate_path declaration source"
compass = ["explain", "validate_path", "--source", "--root", "."]
graphify = ["explain", "validate_path()"]
expect = "answer"
required = ["fn validate_path(v7_checks: bool"]
judgment = "src/routing/path_router.rs:22 declares fn validate_path(v7_checks: bool, path: &str); the answer must contain that declaration."

[[repository.question]]
id = "axum2-callers-validate-path"
kind = "callers"
subject = "validate_path callers"
compass = ["callers", "validate_path"]
graphify = ["affected", "validate_path()", "--depth", "1"]
expect = "answer"
required = ["route"]
judgment = "validate_path is called by PathRouter::route at path_router.rs:71 and PathRouter::route_endpoint at line 125."

[[repository.question]]
id = "axum2-callees-validate-path"
kind = "callees"
subject = "validate_path callees"
compass = ["callees", "validate_path"]
graphify = ["explain", "validate_path()"]
expect = "answer"
required = ["validate_v07_paths"]
judgment = "validate_path calls validate_v07_paths(path) at path_router.rs:31, declared at line 36."

[[repository.question]]
id = "axum2-impact-validate-path"
kind = "impact"
subject = "transitive dependents of validate_path"
compass = ["impact", "validate_path", "--max-depth", "3"]
graphify = ["affected", "validate_path()", "--depth", "3"]
expect = "answer"
max_follow_ups = 3
required = ["route", "route_endpoint"]
judgment = "validate_path is called by PathRouter::route (path_router.rs:71) and PathRouter::route_endpoint (line 125); PathRouter::route is itself called by merge (line 164) and nest (line 201)."

[[repository.question]]
id = "axum2-path-validate-v07"
kind = "path"
subject = "validate_path -> validate_v07_paths"
compass = ["path", "validate_path", "validate_v07_paths"]
graphify = ["path", "validate_path()", "validate_v07_paths()", "--undirected"]
expect = "answer"
required = ["validate_v07_paths"]
judgment = "validate_path calls validate_v07_paths at src/routing/path_router.rs:31."
forbidden = ["NO PATH FOUND", "No directed path found", "No path found"]

[[repository.question]]
id = "axum2-file-path-routing"
kind = "file_path"
subject = "src/routing/mod.rs -> src/routing/path_router.rs"
compass = ["path", "src/routing/mod.rs", "src/routing/path_router.rs"]
graphify = ["path", "routing/mod.rs", "routing/path_router.rs", "--undirected"]
expect = "answer"
required = ["mod.rs", "path_router.rs"]
judgment = "Router::route (routing/mod.rs:192) calls this.path_router.route(path, method_router) at line 194, which is PathRouter::route in routing/path_router.rs:66."
forbidden = ["NO PATH FOUND", "No directed path found", "No path found"]

[[repository.question]]
id = "axum2-ambiguous-with-state"
kind = "ambiguity"
subject = "with_state"
compass = ["search", "with_state"]
graphify = ["explain", "with_state"]
expect = "pick_list"
required_one_of = ["method_routing.rs", "path_router.rs"]
min_one_of = 1
min_candidates = 2
judgment = "with_state is declared by MethodRouter (method_routing.rs:837), Router (routing/mod.rs:444), PathRouter (path_router.rs:305), Handler (handler/mod.rs:202) and Fallback (routing/mod.rs:743); a pick list is required."

[[repository.question]]
id = "axum2-negative"
kind = "negative"
subject = "ZebraQuantumMissingService"
compass = ["search", "ZebraQuantumMissingService"]
graphify = ["explain", "ZebraQuantumMissingService"]
expect = "no_match"
judgment = "The identifier is absent from the pinned checkout; returning candidates as an answer is a false positive."

[[repository.question]]
id = "axum2-broad-routing"
kind = "broad"
subject = "how does axum route an incoming request to a handler"
compass = ["query", "how does axum route an incoming request to a handler"]
graphify = ["query", "how does axum route an incoming request to a handler"]
expect = "answer"
required = ["Router", "MethodRouter"]
budget_tokens = 600
max_follow_ups = 6
judgment = "Router (routing/mod.rs) stores MethodRouter values in its PathRouter, and routing hands the request to the matched MethodRouter."
