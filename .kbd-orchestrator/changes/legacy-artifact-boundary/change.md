# Phase 3 — legacy artifact rebuild boundary

Status: implemented, installed, pushed, and verified with the operator-requested
reduced checks; not independently certified or delivered to fork main.

Reject recognized Compass release artifacts older than 0.3.23 at engine load.
JSON inspects at most 64 KiB before record decoding, full digest computation,
or cached-index reuse. SQLite and Surreal inspect pinned bounded metadata;
native engines must not read portable JSON to enforce this boundary.

Errors include the found release, minimum release, and a force-rebuild command.
Only the selected snapshot's validated sibling source-root.txt may supply the
root; absent/invalid provenance explicitly requires caller input. No in-place
migration and no mutation of historical realizations. Existing non-release
producer labels retain schema validation rather than pretending to be releases.

Production scope: compass-model loaders/error contracts, compass-query engine
and cache boundaries, compatibility/migration/output/security/performance docs.
Force publication already bypasses prior graph reuse; verify it end to end.

Delivery order follows the operator's reduced-verification instruction:
finish production and documentation; build/install the dual-mode release;
commit/push; then run complete integration targets for changed loading paths.
Use an authentic graph generated by the 0.3.6 release. Retain failed attempts
and report deferred broader suites and independent certification accurately.
